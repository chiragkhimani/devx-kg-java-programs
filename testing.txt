const popup = page.locator('#SuccessPopup');
const message = page.locator('#SuccessMessage');

await this.updateEmailButton.click();

await expect(popup).toBeVisible({ timeout: 15000 });
await expect(message).toHaveText('Data Updated Successfully');
